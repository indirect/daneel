require 'daneel'

trap(:INT) do
  puts "Quitting...\n"
  exit 0
end

options = Options.parse(ARGV)

if options.has_key?(:server)
  Server.new(options.delete(:server)).run
end

Daneel::Bot.new(options).run
