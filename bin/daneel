require 'daneel'
require 'daneel/bot'
require 'daneel/options'

trap(:INT) do
  puts "Quitting...\n"
  exit 0
end

options = Daneel::Options.parse(ARGV)

if options.has_key?(:server)
  Daneel::Server.new(options.delete(:server)).run
end

Daneel::Bot.new(options).run
